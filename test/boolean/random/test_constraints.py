import unittest

class TestConstraints(unittest.TestCase):
    def testNum(self):
        #just filler to check that this test gets run with the other unit tests
        x = 3
        self.assertEqual(x, 3)